import 'dart:ui';

class FolderTabConfigurator {
  final Size? size;
  final double radius;
  final int initialTabIndex;
  final double tabHeight;

  FolderTabConfigurator({this.radius = 25.0, this.size = null, this.initialTabIndex = 0, this.tabHeight = 50.0});
}